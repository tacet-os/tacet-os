# yaml-language-server: $schema=https://moonrepo.dev/schemas/project.json
$schema: 'https://moonrepo.dev/schemas/project.json'

# Root project — placeholder so moon recognizes the workspace root.
# Real tasks live in crates/*/moon.yml and packages/*/moon.yml.

type: 'configuration'
language: 'unknown'
